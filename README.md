# FirebaseKit-iOS
A modular, app-agnostic Firebase abstraction layer for iOS (Swift 5.9+). Designed for SwiftUI-first apps but fully usable from UIKit.  FirebaseKit handles initialization, typed access, consistent error mapping, and reusable integration logic. Host apps handle UI, configuration values, model mapping, and app-specific interpretation.
